name: ai_reader_spike
description: "Phase 0 spike: EPUB rendering + selection + Ollama streaming explain."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  epubx: ^4.0.0
  http: ^1.2.0
  file_picker: ^8.0.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
